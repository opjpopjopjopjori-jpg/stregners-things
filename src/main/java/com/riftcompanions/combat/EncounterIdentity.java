package com.riftcompanions.combat;

/** Gameplay identity is a data/profile concern, not a hard-coded companion behavior branch. */
public enum EncounterIdentity {
    UNKNOWN,
    HUNTER,
    SWARM,
    CONTROLLER,
    HEAVY,
    ANCHOR,
    BOSS
}
