package com.riftcompanions.doctrine;

/** Player-approved team rules; no doctrine can bypass the safety gate. */
public enum TeamDoctrine {
    STAY_TOGETHER,
    RETURN_BEFORE_NIGHT,
    ASK_BEFORE_POWERS,
    PROTECT_VILLAGES,
    MARK_SAFE_ROUTES
}
