package com.riftcompanions.identity;

/** Opt-in self-description; it informs presentation and never grants power. */
public enum PlayerPlayStyle {
    UNSET,
    EXPLORER,
    CAUTIOUS,
    BUILDER,
    FIGHTER,
    PROTECTOR
}
