package com.riftcompanions.world;

/** Player-defined places; the mod never assumes a building is a base by appearance. */
public enum BaseAnchorType {
    HOME,
    REST,
    ENTRY,
    GUARD_POST,
    JOURNAL,
    TEAM_SUPPLY,
    LOOKOUT,
    QUIET,
    MEMORIAL
}
