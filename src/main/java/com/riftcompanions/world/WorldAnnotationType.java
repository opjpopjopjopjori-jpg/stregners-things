package com.riftcompanions.world;

/** Player-authored world affordances; no AI guesses a build's meaning from appearance alone. */
public enum WorldAnnotationType {
    SAFE_ROUTE,
    DANGER,
    MACHINE_NO_GO,
    PROTECTED,
    INVESTIGATE
}
