package com.riftcompanions.team;

/** The first three plans are deliberately narrow and have abort/timeout paths. */
public enum TeamPlanType {
    NONE,
    RETREAT,
    DEFEND,
    STRUCTURE_ENTRY
}
