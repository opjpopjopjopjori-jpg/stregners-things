package com.riftcompanions.mode;

/** Performance presentation tier; cinematic does not raise active squad limit without runtime evidence. */
public enum PerformanceTier {
    LIGHT,
    STANDARD,
    CINEMATIC
}
