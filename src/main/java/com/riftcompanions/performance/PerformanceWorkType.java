package com.riftcompanions.performance;

/** Bounded server work categories exposed only through developer diagnostics. */
public enum PerformanceWorkType {
    COMPANION_AI,
    NAVIGATION,
    TEAM_DIRECTOR,
    DIALOGUE
}
