package com.riftcompanions.memory;

/** Low-volume, player-relevant memories only; never one record per mob or block. */
public enum MemoryType {
    SAFE_ROUTE,
    DANGER_ZONE,
    RETREAT,
    STRUCTURE_DISCOVERY,
    ANOMALY_OBSERVATION,
    THREAT_OBSERVATION,
    TEAM_RESCUE,
    MILESTONE,
    RECOVERY,
    DUO,
    INTENTION,
    ARC
}
