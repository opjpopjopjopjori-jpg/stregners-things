package com.riftcompanions.memory;

/** Facts the game can actually observe without reading hidden mod mechanics. */
public enum ThreatObservationKind {
    VISUAL_CONTACT,
    DIRECT_DAMAGE,
    HIVE_TAG_DETECTED
}
